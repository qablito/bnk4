# Apps

Local product surfaces that consume Engine v1 outputs.

Planned:
- local analyzer API
- local analyzer UI

Rule:
- `apps/` depends on engine contracts; contract ownership stays in `engine/` + `docs/ENGINE_V1_CONTRACT.md`.
